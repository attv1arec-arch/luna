# Core image engine — pinned for Debian Bookworm / Python 3.11 compatibility
numpy==1.26.4
Pillow==10.4.0
opencv-python-headless==4.10.0.84
rawpy==0.21.0
scipy==1.14.1
scikit-image==0.24.0
imageio==2.35.1

# Local AI stack actually imported by photo_engine.py
torch==2.4.1
transformers==4.44.2
diffusers==0.30.3
accelerate==0.33.0
safetensors==0.4.5
sentencepiece==0.2.0

# NOTE: realesrgan/basicSR were intentionally removed.
# The current engine uses Transformers Swin2SR for super-resolution,
# and those old packages can fail metadata generation on modern pip/Python.
